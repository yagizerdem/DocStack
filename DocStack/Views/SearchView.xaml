<UserControl x:Class="DocStack.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DocStack.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
            xmlns:converters="clr-namespace:DocStack.Converters"
            xmlns:components="clr-namespace:DocStack.Components"
            xmlns:apiResponseModels="clr-namespace:Models.ApiResponse;assembly=Models">
    <UserControl.Resources>
        <Style  TargetType="TextBlock">
            <Setter Property="Foreground"
            Value="White"/>
            <Setter Property="FontSize"
            Value="16"/>
        </Style>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid Background="{StaticResource MatteBlackBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <!--search bar--> 
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid
                Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="100"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                
                <TextBox 
                    Grid.Column="0"
                    Margin="10" 
              Style="{StaticResource  ModernTextBox}"
              MaxWidth="500"
              VerticalContentAlignment="Center"
              FontWeight="Bold"
              Text="{Binding Query, Mode=TwoWay}"></TextBox>
              
                <Button  Grid.Column="1" 
                         Margin="20 0 0 0"
                         Height="30"
                         Style="{StaticResource ModernButton}"
                         Command="{Binding SearchCommand}">Search</Button>
            
            </Grid>
        </Grid>

        <StackPanel 
            Grid.Row="1"
            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <components:Spinner Margin="0 50"></components:Spinner>
        </StackPanel>

        
        <!--display works--> 
        <Grid Grid.Row="1"
      Visibility="{Binding IsNotLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
      Margin="50">

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ListView ItemsSource="{Binding WorksList}"
              Grid.Row="0"
              Background="Transparent"
              ScrollViewer.VerticalScrollBarVisibility="Visible">

                <ListView.ItemTemplate>
                    <DataTemplate DataType="apiResponseModels:Work">
                        <StackPanel Margin="0,10">
                            <TextBlock Text="abstract"
                               TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding abstract}"
                                    TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </Grid>




    </Grid>
</UserControl>
